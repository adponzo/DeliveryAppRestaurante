import { MinValueValidationDirective } from './min-value-validation.directive';

describe('MinValueValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new MinValueValidationDirective();
    expect(directive).toBeTruthy();
  });
});
